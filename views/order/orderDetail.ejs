<div class='container mt-5'>
    <!-- table -->
    <div class='container'>
        <table class="table table-striped">
            <thead>
            <tr>
                <th scope="col">주문 번호</th>
                <th scope="col">도서 이름</th>
                <th scope="col">도서 수량</th>
                <th scope="col">도서 총액</th>
                <th scope="col"></th>
          </tr>
            </tr>
            </thead>
            <tbody>
            <% for(let orders of order_info){ %>
            <tr>
                <th  style='vertical-align: middle'><%= orders.order_number %></a></th>
                <th  style='vertical-align: middle'><%= book.book_number %></a></th>
                <td  style='vertical-align: middle'><%= orders.order_total %></a></td>
                <td></td>
            </tr>
            <% } %>
            </tbody>
        </table><table class="table table-striped">
        </table>
    </div>
</div>
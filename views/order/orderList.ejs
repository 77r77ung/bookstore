<div class='container mt-5'>
    <!-- table -->
    <div class='container'>
        <table class="table table-striped">
            <thead>
            <tr>
                <th scope="col">주문번호</th>
                <th scope="col">주문일자</th>
                <th scope="col">주문금액</th>
                <th scope="col"></th>
          </tr>
            </tr>
            </thead>
            <tbody>
            <% for(let orders of order_info){ %>
            <tr>
                <th  style='vertical-align: middle'><a href=''><%= orders.order_number %></a></th>
                <td  style='vertical-align: middle'><%= orders.order_date %></a></td>
                <td  style='vertical-align: middle'><%= orders.order_total %></td>
                <td>
                    <div class="btn-group" role="group" aria-label="Basic outlined example">
                        <button type="button" class="btn btn-outline-info" onclick="location.href='/order/deleteOrder/<%= orders.order_number %>'">Delete</button>
                    </div>
                </td>
            </tr>
            <% } %>
            </tbody>
        </table><table class="table table-striped">
        </table>
    </div>
</div>